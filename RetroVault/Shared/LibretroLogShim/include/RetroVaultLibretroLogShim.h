#ifndef RETROVAULT_LIBRETRO_LOG_SHIM_H
#define RETROVAULT_LIBRETRO_LOG_SHIM_H

/// Returns the C variadic callback used by Libretro's logging interface.
void *retrovault_libretro_log_callback_pointer(void);

#endif
