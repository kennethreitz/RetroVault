diff --git a/Source/Core/VideoBackends/OGL/OGLConfig.cpp b/Source/Core/VideoBackends/OGL/OGLConfig.cpp
--- a/Source/Core/VideoBackends/OGL/OGLConfig.cpp
+++ b/Source/Core/VideoBackends/OGL/OGLConfig.cpp
@@ -741,11 +741,9 @@ bool VideoBackend::InitializeGLExtensions()
                   5000);
 
   if (!g_ogl_config.bSupportsGLBufferStorage && !g_ogl_config.bSupportsGLPinnedMemory)
-  {
-    OSD::AddMessage(fmt::format("Your OpenGL driver does not support {}_buffer_storage.",
-                                m_main_gl_context->IsGLES() ? "EXT" : "ARB"),
-                    60000);
-    OSD::AddMessage("This device's performance may be poor.", 60000);
-  }
+    WARN_LOG_FMT(VIDEO,
+                 "OpenGL {}_buffer_storage is unavailable; using Dolphin's compatible streaming "
+                 "fallback.",
+                 m_main_gl_context->IsGLES() ? "EXT" : "ARB");
 
   INFO_LOG_FMT(VIDEO, "Video Info: {}, {}, {}", g_ogl_config.gl_vendor, g_ogl_config.gl_renderer,
                g_ogl_config.gl_version);
