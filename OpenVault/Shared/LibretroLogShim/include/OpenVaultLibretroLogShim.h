#ifndef OPENVAULT_LIBRETRO_LOG_SHIM_H
#define OPENVAULT_LIBRETRO_LOG_SHIM_H

/// Returns the C variadic callback used by Libretro's logging interface.
void *openvault_libretro_log_callback_pointer(void);

#endif
